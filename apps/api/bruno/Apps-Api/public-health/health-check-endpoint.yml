info:
  name: Health check endpoint
  type: http
  seq: 1
http:
  method: GET
  url: "{{host}}/health"
  auth: inherit
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
