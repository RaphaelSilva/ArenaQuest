info:
  name: User Logout
  type: http
  seq: 6
http:
  method: POST
  url: "{{host}}/v1/auth/logout"
  auth: inherit
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
