info:
  name: Initiate Google OAuth Flow
  type: http
  seq: 3
http:
  method: GET
  url: "{{host}}/v1/auth/google"
  auth: inherit
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
