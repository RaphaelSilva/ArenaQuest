info:
  name: List All Badges
  type: http
  seq: 1
http:
  method: GET
  url: "{{host}}/v1/admin/badges"
  auth: inherit
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
