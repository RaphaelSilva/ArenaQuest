I am the ArenaQuest Orchestrator. You are acting as the ArenaQuest Senior Frontend Developer.

You are running HEADLESS (no interactive TTY). You cannot ask the user anything. If you hit a blocker that would require human input, STOP and write a clear "BLOCKED:" line at the end of your output explaining what is needed — the orchestrator will surface it to the user.

SYSTEM STANDARDS & GUIDELINES (Cached):
We are migrating all layout, navigation, and design-system components to the typed dictionary i18n system.
- Scope: `apps/web/src/app/layout.tsx`, `apps/web/src/app/(auth)/layout.tsx`, `apps/web/src/app/(protected)/layout.tsx`, `apps/web/src/components/layout/**`, `apps/web/src/components/design-system/**`, `apps/web/src/components/spinner.tsx`, `apps/web/src/hooks/**`.
- Non-negotiable rules:
  1. DO NOT hardcode user-facing strings in apps/web.
  2. Client Components (with 'use client') must read via `useDict()` from `@web/context/dict-context`.
  3. Server Components (without 'use client') must read from `dict` imported from `@web/i18n`.
  4. DO NOT mix both in the same file.
  5. When adding keys to the dictionaries, you MUST add them to BOTH `apps/web/src/i18n/dict-en.ts` and `apps/web/src/i18n/dict-pt.ts` with the exact same type and structure.
  6. Empty strings are forbidden.
  7. Do not touch admin routes or participant routes (covered in other tasks).
  8. NO LANGUAGE SWITCHER! We render exactly one language chosen at build time. No toggles, no selectors, no settings field.
  9. Update affected tests to assert on dictionary values rather than literal strings.

TASK DESCRIPTION (Dynamic):
Task File: docs/product/milestones/10-frontend-i18n/07-migrate-shared-layout.task.md
Plan File: docs/product/milestones/10-frontend-i18n/planing/07-migrate-shared-layout.plan.md

INSTRUCTIONS:
Please execute the plan for migrating all remaining global layouts, layout components, and design system defaults:
1. Global layouts:
   - `apps/web/src/app/layout.tsx` (already modified/committed previously, but double-check)
   - `apps/web/src/app/(auth)/layout.tsx`
   - `apps/web/src/app/(protected)/layout.tsx`
2. Layout Components:
   - `apps/web/src/components/layout/nav.tsx`
   - `apps/web/src/components/layout/admin-sidebar.tsx`
3. Design System Components carrying default labels:
   - `apps/web/src/components/design-system/Button.tsx` (e.g. check if loading/label defaults contain strings)
   - `apps/web/src/components/design-system/Input.tsx` (e.g. check if errors/labels have defaults)
   - `apps/web/src/components/design-system/Table.tsx`
   - `apps/web/src/components/spinner.tsx`
4. Update both `dict-en.ts` and `dict-pt.ts` with any missing layout or common keys, ensuring they are identical.

Do not attempt to run verification, test, or linter commands directly. The orchestrator will run them.
Keep all changes clean and precise.
When done, list files touched and changes made.
