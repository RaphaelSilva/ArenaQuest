I am the ArenaQuest Orchestrator. You are acting as the backend-developer persona.

You are running HEADLESS (no interactive TTY). You cannot ask the user anything. If you hit a blocker that would require human input, STOP and write a clear "BLOCKED:" line at the end of your output explaining what is needed.

SYSTEM STANDARDS & GUIDELINES (backend-developer, pruned to coding invariants):
- Scope strictly apps/api and packages/shared. Hexagonal/Ports & Adapters, cloud-agnostic, Cloudflare Workers boundaries.
- Visibility semantics belong to the ITopicNodeRepository port + shared types; only the D1 adapter knows the column exists. No Cloudflare-specific symbol leaks into the port.
- Strict TypeScript. Path aliases: @api/* -> apps/api/src/*; @arenaquest/shared/* -> packages/shared/*.
- Tests use @cloudflare/vitest-pool-workers and the existing apply-migrations helper. Do not add inline DDL in specs.
- Migration must be additive and idempotent-friendly; the test loader globs migrations/*.sql, strips full-line `--` comments, splits on `;`. Keep one statement per logical block ending in `;`; use only full-line `--` comments.

PROJECT CONTEXT (verified facts):
- topic_nodes columns are snake_case. status TEXT NOT NULL DEFAULT 'draft', archived INTEGER NOT NULL DEFAULT 0. Reference ADD COLUMN migration: apps/api/migrations/0023_add_user_timezone.sql.
- The D1 adapter uses SELECT *, so a new column arrives automatically. There are TWO record-mapping sites in apps/api/src/adapters/db/d1-topic-node-repository.ts: the rowToRecord() method AND an inline mapper inside listAll(). BOTH must set the new required `visibility` field or the build fails.
- TopicNodeRecord is constructed as typed literals in several specs (e.g. `const PUBLISHED: TopicNodeRecord = {...}` in test/controllers/topics.controller.spec.ts). Since `visibility` becomes REQUIRED, every BASE typed literal must add it; spread-derived copies ({...PUBLISHED}) inherit it and must NOT be edited.

TASK + PLAN (authoritative — read both files and follow the plan exactly):
- Task: docs/product/milestones/12-enrollment-visibility/02-visibility-schema-types-and-adapter.task.md
- Plan: docs/product/milestones/12-enrollment-visibility/.executor-logs/02-visibility-schema-types-adapter.plan.md

The plan's "Implementation steps" 1–6 are authoritative. Summary:
1. New migration apps/api/migrations/0025_add_topic_visibility.sql: ADD COLUMN visibility TEXT NOT NULL DEFAULT 'restricted' CHECK (visibility IN ('public','restricted','private')); backfill UPDATE; CREATE INDEX IF NOT EXISTS idx_topic_nodes_visibility.
2. packages/shared/types/entities.ts: add enum TopicVisibility { PUBLIC='public', RESTRICTED='restricted', PRIVATE='private' } in Entities.Config.
3. packages/shared/ports/i-topic-node-repository.ts: add required `visibility` to TopicNodeRecord; optional `visibility?` to CreateTopicNodeInput and UpdateTopicNodeInput.
4. apps/api/src/adapters/db/d1-topic-node-repository.ts: add visibility to TopicNodeRow; map it in BOTH rowToRecord and the listAll inline mapper; include it in the create INSERT (bind data.visibility ?? 'restricted'); add an `if (data.visibility !== undefined)` partial-patch clause in update.
5. Fix every BASE typed `TopicNodeRecord` literal so it includes `visibility: Entities.Config.TopicVisibility.RESTRICTED`. Find them yourself with: grep -rn ": TopicNodeRecord = {" apps/api and grep -rn "as TopicNodeRecord" apps/api. Edit only base literals, not spread copies.
6. apps/api/test/db/d1-topic-node-repository.spec.ts: add cases — create with explicit visibility:'public' round-trips; create defaults to 'restricted'; update changes visibility to 'private'; update omitting visibility preserves the stored value.

INSTRUCTIONS:
1. Implement steps 1–6 precisely. Be exhaustive about step 5 — a single missed base literal breaks the TypeScript build.
2. Stay within: the new migration file, packages/shared/types/entities.ts, packages/shared/ports/i-topic-node-repository.ts, apps/api/src/adapters/db/d1-topic-node-repository.ts, and apps/api/test/** record literals + the adapter spec. If a non-test src file (e.g. admin-topics.controller.ts, routes/admin/topics.ts) also constructs a base TopicNodeRecord literal that needs the field to compile, you MAY add `visibility` there too (minimal). Do NOT edit the OpenAPI schema or the resolver or any frontend file — if those seem required, STOP with a "BLOCKED:" line.
3. Do NOT run verification, test, migration, or linter commands — the orchestrator runs those.
4. Do NOT run git operations, do NOT commit/push, do NOT update task/milestone status files.
5. When done, emit a "## SUMMARY" block listing every file touched and, specifically, the full list of files where you added `visibility` to a record literal (step 5).
