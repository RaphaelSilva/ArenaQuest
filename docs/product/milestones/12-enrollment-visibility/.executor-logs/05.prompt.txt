I am the ArenaQuest Orchestrator. You are acting as the frontend-developer persona.

You are running HEADLESS (no interactive TTY). You cannot ask the user anything. If you hit a blocker that would require human input, STOP and write a clear "BLOCKED:" line at the end of your output.

SYSTEM STANDARDS & GUIDELINES (frontend-developer, pruned):
- Scope strictly apps/web. Next.js 15 App Router, React 19, Tailwind v4. Path alias @web/* -> apps/web/src/*.
- Dictionary-driven copy ONLY: no hardcoded user-facing strings under src/{app,components,hooks}/**. Client components read copy via useDict(). A key must exist in BOTH dict-en.ts and dict-pt.ts (no empty strings).
- Design tokens only — reuse the existing Tailwind classes already used by the adjacent control; do not invent hex/px.
- API calls go through the existing client (client.adminTopics.update). Types come from apps/web/src/lib/admin-topics-api.ts (local string-literal unions), not the shared port.
- Server components by default; this page is already a 'use client' component, so just extend it.

CONTEXT FACTS (verified):
- File apps/web/src/app/(protected)/admin/topics/page.tsx is a client component. In AdminTopicsPage, `d = dict.admin.topics`. The detail pane has a STATUS select (~line 884) bound to `detailStatus` state (~line 256), synced from selectedNode.status (~line 320; default ~line 311), and saved in handleDetailSave via client.adminTopics.update(selectedId, { ..., status: detailStatus, ... }) (~line 481). Mirror this exact pattern for visibility.
- The Dictionary type is Broaden<typeof dictPt> (apps/web/src/i18n/types.ts). dict-pt.ts is canonical and drives the type; dict-en.ts must mirror its shape or tsc fails. Add keys under the admin.topics.detail block (the one with statusLabel/statusDraft/statusPublished/statusArchived). No types.ts edit needed.
- make test-web runs scripts/check-i18n-coverage.js then vitest; new copy MUST be dict-sourced.
- Visibility values: 'public' | 'restricted' | 'private'; default to 'restricted' when a node's value is absent.

TASK + PLAN (authoritative — read both and follow exactly):
- Task: docs/product/milestones/12-enrollment-visibility/05-topic-editor-visibility-selector.task.md
- Plan: docs/product/milestones/12-enrollment-visibility/.executor-logs/05-topic-visibility-selector.plan.md

Implementation summary (plan steps 1–4 authoritative):
1. admin-topics-api.ts: add `visibility: 'public'|'restricted'|'private'` to TopicNode; add optional `visibility?` to UpdateTopicInput and CreateTopicInput.
2. dict-pt.ts THEN dict-en.ts: under admin.topics.detail add visibilityLabel, visibilityPublic, visibilityRestricted, visibilityPrivate, and one visibilityHelp string explaining the three levels. Translate fully in both languages.
3. page.tsx: add detailVisibility state (default 'restricted'); sync it from selectedNode.visibility (?? 'restricted') alongside the other detail syncs and reset in the no-selection branch; add `visibility: detailVisibility` to the handleDetailSave update payload; add a new <div> block right AFTER the status select mirroring its markup (label + select with three options using d.detail.visibility* + a small help <p> using d.detail.visibilityHelp), reusing the status select's className tokens.
4. New test apps/web/src/lib/__tests__/admin-topics-api.test.ts (mirror topics-api.test.ts): mock the http transport and assert update(id, { visibility: 'public' }) issues PATCH /admin/topics/:id with a body containing visibility:'public'. A full page-render test is OPTIONAL; skip it if it needs heavy provider scaffolding.

INSTRUCTIONS:
1. Implement steps 1–4 precisely, mirroring the existing status-select pattern for visual + behavioural consistency.
2. Stay within the files named above. Do NOT touch backend files, the Access page, detail pages, or the participant catalog. If such an edit seems required, STOP with a "BLOCKED:" line.
3. Do NOT run verification/test/lint commands; do NOT run git; do NOT update task/status files.
4. When done, emit a "## SUMMARY" block listing files touched, the new dict keys added (with PT+EN values), and the test added.
