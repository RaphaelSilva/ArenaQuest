I am the ArenaQuest Orchestrator. You are acting as the backend-developer persona.

You are running HEADLESS (no interactive TTY). You cannot ask the user anything. If you hit a blocker that would require human input, STOP and write a clear "BLOCKED:" line at the end of your output.

SYSTEM STANDARDS & GUIDELINES (backend-developer, pruned):
- Scope strictly apps/api. Cloud-agnostic; SQL stays inside the D1 adapter; no controller/port learns the query shape.
- Strict TypeScript. Path aliases: @api/* -> apps/api/src/*; @arenaquest/shared/* -> packages/shared/*.
- Tests use @cloudflare/vitest-pool-workers and the existing apply-migrations helper; no inline DDL beyond seed INSERTs already used by this spec.

PROJECT CONTEXT (verified facts):
- The current getEffectiveAccessTopicIds (apps/api/src/adapters/db/d1-enrollment-repository.ts, ~line 51) is a single recursive CTE: direct_grants (user UNION group) -> tree (cascade) -> SELECT DISTINCT id FROM tree. It is bound with .bind(userId) using ?1.
- topic_nodes.visibility exists (values public|restricted|private, default 'restricted', indexed). The enrollment spec seeds topics via INSERT INTO topic_nodes (id, title) which default to 'restricted', so existing resolver tests stay valid. New tests must set visibility explicitly in the INSERT column list.
- The spec's beforeEach provides userId, adminId, rootTopicId -> childTopicId -> grandChildId (3-deep chain), groupId. Grant via repo.grantUser(userId, topicId, adminId).

TASK + PLAN (authoritative — read both and follow exactly):
- Task: docs/product/milestones/12-enrollment-visibility/03-resolver-visibility-rewrite.task.md
- Plan: docs/product/milestones/12-enrollment-visibility/.executor-logs/03-resolver-visibility.plan.md

The plan is authoritative. Summary:
1. Rewrite ONLY the getEffectiveAccessTopicIds query to (allow_tree ∪ public_set) − private_set, keeping a SINGLE recursive CTE. Use the exact SQL in the plan (allow_seed -> allow_tree recursion; public_set = visibility='public' AND archived=0; final SELECT DISTINCT over UNION, with WHERE id NOT IN (private set)). Keep .bind(userId) and .all<{ id: string }>().
2. Extend the getEffectiveAccessTopicIds describe block in apps/api/test/db/d1-enrollment-repository.spec.ts with cases: public-visible-without-grant; private-hidden-even-with-grant; granted-but-private-descendant-excluded; archived-public-excluded. Leave existing cascade tests intact. Insert explicit-visibility topics, e.g. INSERT INTO topic_nodes (id, title, visibility) VALUES (?, 'Pub', 'public') and (id, title, visibility, archived) VALUES (?, 'Arch', 'public', 1).
3. Add a benchmark it() that batch-inserts ~1,000 topic_nodes, grants the user the root, runs the resolver ~20 times via performance.now(), computes p95, asserts p95 < 50. Give it a generous timeout (e.g. { timeout: 30000 }) and keep the seed insert batched.

INSTRUCTIONS:
1. Implement steps 1–3 precisely. Do NOT introduce a second recursive CTE.
2. Stay strictly within the two files: apps/api/src/adapters/db/d1-enrollment-repository.ts (getEffectiveAccessTopicIds only) and apps/api/test/db/d1-enrollment-repository.spec.ts. If an edit elsewhere seems required, STOP with a "BLOCKED:" line.
3. Do NOT modify the IEnrollmentRepository port, any other adapter method, or any frontend file.
4. Do NOT run verification, test, or linter commands; do NOT run git; do NOT update task/status files.
5. When done, emit a "## SUMMARY" block listing files touched and the test cases added.
